# Claude Skills 装备栏管理页面 — 对话记录与需求设计

> 本文件由 Claude Code 会话于 2026-05-19 整理，用于在新窗口（本目录下）继续开发。

---

## 一、原始需求（用户原话）

> 可否做一个本地的页面，用于管理 Claude 的 skills。
> 这个页面左边是 skills 的名称和简介，鼠标放上去是 skill 的详细介绍。
> 然后右边是类似游戏（比如暗黑破坏神）的装备栏。可以左边的 skills 拖到右边生效。

---

## 二、需求理解

做一个本地网页（暗黑破坏神风格）：

- **左侧**：skill 列表，名称 + 一句话简介，鼠标 hover 弹出详细介绍（读 SKILL.md 的 frontmatter）。
- **右侧**：装备栏格子，从左边拖 skill 进格子 = "装备"，拖出 = "卸下"。

**核心动机（用户补充）**：这个工具让用户管理"使用哪些 skills、不使用哪些 skills"。
现在 skill 少，多了以后很难管理；有了这个工具就能管理了。

---

## 三、关键决策（已与用户确认）

通过 AskUserQuestion 确认的三个问题：

| 问题 | 用户选择 |
|------|----------|
| 拖进装备栏"生效"是真实开关还是纯可视化 | **真实生效** |
| 管理范围 | **只管个人 skill**（`~/.claude/skills/`，不含 plugin skill） |
| 装备栏格子 | **固定格子**（游戏感强） |

---

## 四、最终设计方案（动手前已向用户说明）

### 生效机制 —— 移动文件（不用软链接）

- 装备的 skill 物理存放在 `~/.claude/skills/`（Claude 真正读取的目录）。
- 卸下的 skill 移动到 `~/.claude/skills-armory/`（仓库，Claude 不读）。
- 拖进格子 = 把目录从 armory 移回 skills；拖出 = 从 skills 移到 armory。
- **不用软链接**：无法 100% 确定 Claude Code 会跟随 symlink 发现 skill；移动文件绝对可靠。
- **不做一次性迁移**：armory 目录在首次"卸下"时才创建。当前 7 个 skill 全部是"已装备"状态，
  用户不操作就不动任何文件。

### skill 判定规则

- 只有包含 `SKILL.md` 的目录才算 skill。
- `octopus-research-workspace` 没有 SKILL.md → 自动忽略，绝不触碰。

### 装备栏

- 固定 **12 格**（足够当前 7 个，留扩展空间）。
- 格子数做成代码顶部一个常量，以后想收紧改一个数字即可。

### 技术形态

- 单文件 Python 服务（`http.server`，零第三方依赖），一条命令启动，浏览器打开。
- 纯 HTML 无法读文件系统、无法移动文件，所以必须有本地后端。

---

## 五、本地 skill 现状（扫描结果）

`~/.claude/skills/` 下的目录：

| 目录 | 是否 skill | 说明 |
|------|-----------|------|
| `deep-research` | ✅ | 定向深度搜索研究助理 |
| `lab-to-prod` | ✅ | 研发策略 → 生产策略转换 |
| `local-data-guide` | ✅ | 本地 MySQL 数据获取指南 |
| `octopus-research` | ✅ | 递归式深度研究助理（八爪鱼） |
| `octopus-research-workspace` | ❌ | 无 SKILL.md，是工作区，不管理 |
| `skill-creator` | ✅ | 创建/优化 skill |
| `superpowers` | ✅ | 结构化开发流程 |

**SKILL.md frontmatter 格式**（YAML，位于开头两个 `---` 之间）：

```yaml
---
name: deep-research
description: 定向深度搜索研究助理。当用户说"帮我搜索"...（可能很长，单行；个别带引号）
version: 0.2.0   # 可选
---
```

- `description` 字段一般为单行，可能被双引号包裹（如 superpowers）。
- 卡片简介取 description 第一句并截断；hover tooltip 显示完整 description。

### 依赖注意

`octopus-research` 的 SKILL.md 里用绝对路径引用了 `~/.claude/skills/deep-research/SKILL.md`。
若卸下 `deep-research` 而保留 `octopus-research`，后者会失效——这是用户自行取舍，工具不强制拦截，
但可在 UI 上做温和提示（可选）。

---

## 六、待实现的服务设计（实现蓝图）

### 路径常量

```python
SKILLS_DIR  = ~/.claude/skills          # 已装备（Claude 读取）
ARMORY_DIR  = ~/.claude/skills-armory   # 仓库（卸下的，首次卸下时创建）
LAYOUT_FILE = ~/.claude/skill-manager-layout.json   # 仅记录格子位置
NUM_SLOTS   = 12
PORT        = 8777
```

- "是否装备"的真相 = 目录在 SKILLS_DIR 还是 ARMORY_DIR；`layout.json` 只记录格子位置（视觉）。

### 后端 API

| 方法/路径 | 作用 |
|-----------|------|
| `GET  /` | 返回页面 HTML |
| `GET  /api/skills` | 返回 `{skills:[{dir,name,description,equipped}], layout:{slot:dir}, num_slots}` |
| `POST /api/equip` `{dir, slot}` | 目录 armory→skills；写 layout；装备栏满则报错 |
| `POST /api/unequip` `{dir}` | 目录 skills→armory；从 layout 移除 |
| `POST /api/arrange` `{layout}` | 仅调整格子位置（同栏内拖动/交换），不动文件 |

### 安全要点

- `dir` 必须匹配 `^[A-Za-z0-9._-]+$`，防路径穿越。
- 移动前校验源目录含 SKILL.md、目标目录不存在（不覆盖、不删除）。
- equip 前先检查有空格子，没有则报错"装备栏已满"，再移动文件。

### 前端（暗黑破坏神风格）

- 深色哥特风：暗背景 + 金色描边，serif 字体（含中文 Noto Serif CJK）。
- 左：技能"典籍/仓库"列表，仅显示**未装备**的 skill；卡片可拖拽；hover 出 tooltip。
- 右：12 格装备栏（3×4），格子空/满两种态；已装备 skill 可拖拽。
- HTML5 拖拽：左→空格子 = 装备；格子→左面板 = 卸下；格子↔格子 = 换位/交换。
- 每个操作后重新 `GET /api/skills` 渲染，避免状态漂移。
- 单 tooltip div 跟随鼠标显示完整 description；toast 显示错误。
- skill 图标 emoji 映射：deep-research🔍 / octopus-research🐙 / local-data-guide🗄️ /
  lab-to-prod🚀 / skill-creator🛠️ / superpowers⚡ / 默认📜。

### 生效时机说明（UI 上提示）

Claude Code 在会话启动时加载 skill，装备/卸下后**需重启 Claude 会话才生效**。

---

## 七、下一步

在本目录（`/home/roy/Documents/Epiphany/SkillsEquipmentSlots/`）下新建：

- `server.py` —— 单文件服务（后端 API + 内嵌前端 HTML）。

启动方式：`python3 server.py` → 浏览器打开 `http://127.0.0.1:8777`。
